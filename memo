En lisant le Readme.md


Réalise le logiciel implementant le node.
Met le code dans firmware/
Il est basé sur l'exploitation d'une carte Arduino Nano (ATmega328P) avec recepteur radio intégré (RF-Nano for Arduino Nano V3.0, Micro USB Nano Board ATmega328P QFN32 5V 16M CH340, Integrate NRF24L01+2.4G wireless - https://fr.aliexpress.com/item/1005005593392265.html?spm=a2g0o.order_list.order_list_main.17.4d7d1802i1MZ0a&gatewayAdapt=glo2fra).
Fait un projet arduino - compilable et flashable en CLI.

Coté pinout - il doit être possible (via jumper sur les IO ) d'activer un mode test - qui fait un chenillard sur les 10 leds.
Coté pinout - il doit être possible (via jumper sur les IO ) de paramétrer une adresse - permettant de matérialiser quel est le numéro du recepteur parmis les 60 musiciens.
Le changement d'une pin est immédiatement inerprété par le soft - pas besoin de reset l'appi

Pour la gestion des leds utilise une lib (dispo dans la collection arduino)
Pour la gestion de la radio une lib (dispo dans la collection arduino)

Exploite un uart pour faire des logs de trace avec formatage
Au démarrage affiche le nom et la version de l'appli

Coté reception trame radio.
Sera émis par la gateway une trame de 512 octet par univers (afin de ne pas saturer la petite ram de l'ATmega328P).
le paquet est structuré de la façon suivante
<magic_paquet> ["START_LEDS"] | uint-16 <paquet_len> | uint_8 <id_univers> | 512 octect max <valeurs_DMX> | uint_32 <CRC32>

Attention la taille de taille de trame n'es pas fixe (par example pour l'univers 4 il n'y a pas 512 octets) 

le paquet_len contient la taille complete du paquet : magic_paquet + paquet_len + id_univers + valeurs_DMX + CRC32

Le node reçoit la trame , detecte le magic paquet , extrait la longueur, récupère les octets restant et fait un check CRC.
Une fois la trame validé, le node identifie si le paquet lui est destiné - en comparant son adresse avec le contenu du paquet.
Afin de determiner si son adresse matche avec la data du paquet - il exploite le id_univers et le paquet_len.
exemple , le node a l'adresse 42.
le nombre de led par node est 10.
le premier paquet a un id_univers=0 et un paquet_len de 

Le parseur fonctionne en octet par octet.

En cas de non reception de paquet après plus de 2 secondes - toutes les leds sont remise dans leur état initial - à savoir tout éteinds.


Ajoute des éléments si j'en ai oublié


